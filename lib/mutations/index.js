const { addChef } = require('./ChefMutation');
const { addDish } = require('./DishMutation');
const { addCharacter } = require('./CharacterMutation');

module.exports = {
  addChef,
  addDish,
  addCharacter
};
